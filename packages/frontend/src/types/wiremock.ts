// WireMock protocol and stub test types are defined in @wiremock-hub/shared
// (single source of truth shared with the backend)
export type {
  Mapping,
  MappingRequest,
  MappingResponse,
  BodyPattern,
  LoggedRequest,
  MappingsResponse,
  RequestsResponse,
  StubTestRequest,
  StubTestInstanceResult,
  StubTestResponse
} from '@wiremock-hub/shared';
